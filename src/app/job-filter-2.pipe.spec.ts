import { JobFilter2Pipe } from './job-filter-2.pipe';

describe('JobFilter2Pipe', () => {
  it('create an instance', () => {
    const pipe = new JobFilter2Pipe();
    expect(pipe).toBeTruthy();
  });
});
